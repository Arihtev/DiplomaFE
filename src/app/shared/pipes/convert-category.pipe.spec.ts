import { ConvertCategoryPipe } from './convert-category.pipe';

describe('ConvertCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
